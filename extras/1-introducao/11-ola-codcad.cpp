// Olá CodCad!
// Esse é o primeiro problema que você deve resolver. Normalmente problemas terão uma entrada e uma saída esperada, porém como nós ainda não aprendemos como fazer a entrada de dados para o computador, você deve apenas imprimir uma mensagem na tela do computador.

// Dessa forma, para resolver esse problema você deve apenas imprimir a mensagem "Ola CodCad!" na tela do computador.

// Para enviar o código contendo sua solução, você deve usar os botões que se encontram no topo dessa página. O botão para enviar sua solução é identificado pelo símbolo do avião de papel (). Você também pode pedir ajuda na secção dos comentários abaixo ou visualizar todas as suas soluções para esse problema utilizando o botão do quebra-cabeça ().

// Entrada
// Nesse problema especial você não precisa ler nenhuma informação como entrada.

// Saida
// A saída do seu programa deve ser apenas a frase "Ola CodCad!" (sem as aspas), conforme aprendemos na primeira lição. Note que a palavra "Ola" não tem acento e note também o "!" ao final da frase. Seu programa deve imprimir exatamente como solicitado na questão.

// Exemplos de Entrada	Exemplos de Saída
// Ola CodCad!

#include <iostream>

int main() {
    std::cout << "Ola CodCad!" << std::endl;
    return 0;
}